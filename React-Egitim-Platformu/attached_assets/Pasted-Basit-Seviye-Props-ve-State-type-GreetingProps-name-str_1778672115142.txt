Basit Seviye – Props ve State
type GreetingProps = { name: string; age?: number };

export function Greeting({ name, age }: GreetingProps) {
  return (
    <div>
      <h2>Hello, {name}!</h2>
      {age && <p>You are {age} years old</p>}
    </div>
  );
}

// Kullanım:
<Greeting name="Emirhan" age={20} />


👉 Props ve opsiyonel tip kullanımı gösteriyor.

🟡 Orta Seviye – API Çağrısı ve useEffect
import React, { useState, useEffect } from "react";

type Post = { id: number; title: string };

const Posts: React.FC = () => {
  const [posts, setPosts] = useState<Post[]>([]);

  useEffect(() => {
    fetch("https://jsonplaceholder.typicode.com/posts?_limit=5")
      .then(res => res.json())
      .then(data => setPosts(data));
  }, []);

  return (
    <section>
      <h2>API'den Gelen Veriler</h2>
      <ul>
        {posts.map(post => (
          <li key={post.id}>{post.title}</li>
        ))}
      </ul>
    </section>
  );
};

export default Posts;


👉 useEffect ile veri çekme ve tipli state kullanımı.

🔴 İleri Seviye – Context API + Custom Hook
import React, { createContext, useContext, useState } from "react";

type Theme = "light" | "dark";
type ThemeContextType = { theme: Theme; toggleTheme: () => void };

const ThemeContext = createContext<ThemeContextType | undefined>(undefined);

export const ThemeProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [theme, setTheme] = useState<Theme>("light");
  const toggleTheme = () => setTheme(theme === "light" ? "dark" : "light");

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme must be used within ThemeProvider");
  return ctx;
};

// Kullanım:
const ThemeButton: React.FC = () => {
  const { theme, toggleTheme } = useTheme();
  return (
    <button onClick={toggleTheme}>
      Şu anki tema: {theme}
    </button>
  );
};


👉 Context API + custom hook ile tema yönetimi.

🎨 Görseller/Diyagramlar İçin
- Recharts → grafikler (npm install recharts)
- React Flow → component tree veya data flow diyagramı (npm install reactflow)
- Nivo → estetik chart kütüphanesi (npm install nivo)

📌 Bu üç seviye örneği sitende “Basit”, “Orta”, “İleri” başlıkları altında gösterebilirsin. Böylece kullanıcı hem temel hem de ileri düzey React + TSX kullanımını öğrenir.
👉 İstersen sana bu örnekleri Content.tsx içinde sekmeli (Tabs) yapıda gösterecek hazır kod da verebilirim. Böylece kullanıcı sekmeler arasında geçiş yaparak farklı seviyeleri görebilir. İlgini çeker mi?
